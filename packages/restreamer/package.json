{
  "name": "@av/restreamer",
  "version": "0.2.0",
  "private": true,
  "description": "Portable datarhei Core / Restreamer client + split-channel manager (fan a single ingest out to a monitor copy plus N egress destinations). No framework coupling — drop into any Node app.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "src", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.7.2"
  }
}
